# Infra Notes

Local infrastructure targets for next phase:
- Postgres for durable account/order/ledger data
- Redis for cache, rate limits, and websocket fanout support
- Later: object storage for chart/history exports, telemetry backend, worker queue
